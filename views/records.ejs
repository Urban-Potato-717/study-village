<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <title>내 공부 기록 - Study Village</title>
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
  <h1>내 공부 기록</h1>

  <!-- 임태균 담당: records 배열을 표로 표시 -->
  <table>
    <thead>
      <tr>
        <th>시작</th>
        <th>종료</th>
        <th>공부 시간(초)</th>
      </tr>
    </thead>
    <tbody>
      <% if (records.length === 0) { %>
        <tr><td colspan="3">아직 기록이 없습니다.</td></tr>
      <% } else { %>
        <% records.forEach(function (r) { %>
          <tr>
            <td><%= r.start_time %></td>
            <td><%= r.end_time %></td>
            <td><%= r.duration %></td>
          </tr>
        <% }); %>
      <% } %>
    </tbody>
  </table>

  <p><a href="/">메인으로</a></p>
</body>
</html>
